import React from 'react';
import { Text, Link, VStack, HStack, Button, Image, Box, Input } from 'native-base';

export default function Password({ route, navigation }: any) {
    return (
        <Box justifyContent="center" alignItems="center" flex={1}>
            <Text>Password</Text>
        </Box>
    )
}